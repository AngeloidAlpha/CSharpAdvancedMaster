
int[] range = Console.ReadLine()
    .Split()
    .Select(int.Parse)
    .ToArray();

List<int> numbers = new List<int>();
int start = numbers[0];
int end = numbers[1];

for (int i = start; i < end; i++)
{

}